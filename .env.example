# Required to run the agent
OPENAI_API_KEY=sk-...

# Optional: model (default gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Optional: config file path (default ~/.emergent-mind/config.json)
# EMERGENT_MIND_CONFIG=/path/to/config.json
